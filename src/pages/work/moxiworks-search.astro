---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title="Home Search Redesign — Dustin Martinka"
      description="How I redesigned home search for millions of buyers — and the decisions that got us there."
    />
  </head>
  <body>

    <div class="bg-layer" aria-hidden="true">
      <div class="bg-orbs">
        <div class="orb orb-a"></div>
        <div class="orb orb-b"></div>
        <div class="orb orb-c"></div>
      </div>
      <div class="bg-grain"></div>
    </div>

    <Header forceScrolled={true} />

    <div class="cs-content">

      <!-- BACK LINK -->
      <div class="back-wrap">
        <div class="page-container">
          <a class="back-link" href="/work" id="back-btn">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M9 2L4 7l5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            All work
          </a>
        </div>
      </div>

      <!-- HERO -->
      <section class="hero">
        <div class="page-container hero-inner">
          <div class="hero-text">
            <p class="eyebrow">Case Study · MoxiWorks · Dec 2017 – Mar 2018</p>
            <h1 class="hero-headline">
              Redesigned home search<br>
              for <em>millions of buyers.</em>
            </h1>
            <div class="meta-strip">
              <div class="meta-item">
                <p class="meta-label">My role</p>
                <p class="meta-value">Lead UX Designer</p>
              </div>
              <div class="meta-divider" aria-hidden="true"></div>
              <div class="meta-item">
                <p class="meta-label">Team</p>
                <p class="meta-value">Designer, PM, Dev, QA</p>
              </div>
              <div class="meta-divider" aria-hidden="true"></div>
              <div class="meta-item">
                <p class="meta-label">Timeline</p>
                <p class="meta-value">4 months</p>
              </div>
            </div>
          </div>
          <div class="hero-phone">
            <img src="/work/search/hero-img.png" alt="MoxiWorks home search redesign — final UI" class="hero-phone-img" loading="eager" />
          </div>
        </div>
      </section>

      <!-- CONTEXT -->
      <section class="section section--light">
        <div class="section-inner two-col">
          <div class="col-left">
            <h2 class="section-heading">The context</h2>
            <p class="body-text">Brokerage websites were losing. Zillow, Redfin, and Realtor.com kept raising the bar on search, and traditional brokerages couldn't compete on lead volume. But there was a real opportunity: improve the relationship between agents and their clients.</p>
            <p class="body-text">MoxiWorks' search tool was embedded in brokerage sites used by agents and buyers across the country. It was outdated, slow, and mobile experience was a consistent complaint agents heard from clients. We had to fix it.</p>
          </div>
          <div class="col-right">
            <div class="objective-box">
              <p class="objective-label">The goal</p>
              <p class="objective-quote">"Deliver an improved home search experience that helps agents and clients collaborate — and gives our sales team something to talk about."</p>
            </div>
          </div>
        </div>
        <div class="section-inner" style="margin-top: 64px;">
          <figure class="full-bleed-fig">
            <img src="/work/search/before.png" alt="The existing search interface — cluttered navigation, too many controls, overwhelming to use" class="full-bleed-img" loading="lazy" />
            <figcaption>The existing interface. So much functionality had been added over time that it had become difficult to know where to start.</figcaption>
          </figure>
        </div>
      </section>

      <!-- DECISION 1 -->
      <section class="section section--dark">
        <div class="section-inner">
          <p class="section-eyebrow">Decision 01</p>
          <h2 class="section-heading-dark">Declared the product was for consumers, not agents — before a single screen was designed.</h2>
          <div class="decision-body">
            <p class="body-text body-text--muted">The search tool was originally built for agents. That shaped every decision in it — the feature set, the information hierarchy, the complexity.</p>
            <p class="body-text body-text--muted">The first thing we did was get stakeholder alignment on who we were actually designing for. We made the call up front: this is a consumer product. That single declaration changed what we kept, what we cut, and what we prioritized.</p>
            <p class="body-text body-text--muted">It also meant some stakeholders had to let go of features they cared about. We got ahead of that early.</p>
          </div>
        </div>
      </section>

      <!-- RESEARCH INSIGHTS -->
      <section class="section section--light">
        <div class="section-inner">
          <p class="section-eyebrow-light">What research told us</p>
          <h2 class="section-heading">Competition showed us the bar. Stakeholders told us where we were falling short. Data told us what to cut.</h2>
          <p class="body-text" style="max-width: 680px; margin-bottom: 48px;">We were a scrappy team — no dedicated researcher, no big budget. So we divided the work: competitive analysis across Zillow, Redfin, Airbnb, and HomeAway; PM-led stakeholder interviews with Sales, Account Management, and key brokerage clients; and a review of every existing feature to find what wasn't being used.</p>
          <div class="insights-grid">
            <div class="insight-item">
              <p class="insight-label">From buyers</p>
              <p class="insight-title">Proximity drives decisions</p>
              <p class="insight-body">The 4 top homebuyer priorities: neighborhood quality, affordability, proximity to work, proximity to family and friends. Location wasn't just a filter — it was the whole job.</p>
            </div>
            <div class="insight-item">
              <p class="insight-label">From stakeholders</p>
              <p class="insight-title">Mobile was broken</p>
              <p class="insight-body">Every internal and external stakeholder said the same thing first: mobile experience is failing. Agents were hearing it directly from clients. That set the priority order immediately.</p>
            </div>
            <div class="insight-item">
              <p class="insight-label">From the data</p>
              <p class="insight-title">3 features had near-zero adoption</p>
              <p class="insight-body">Search by MLS#, search by MLS area, and set border radius. We cut all three. Less complexity, cleaner experience — and no one missed them.</p>
            </div>
            <div class="insight-item">
              <p class="insight-label">From competitors</p>
              <p class="insight-title">The bar had already been set</p>
              <p class="insight-body">Inline photo galleries (Airbnb), prices on the map (Airbnb), listing card overlays (Zillow), draw-your-own-boundary (Redfin). We weren't inventing the space — we were catching up to it.</p>
            </div>
          </div>
          <div class="comp-grid" style="margin-top: 56px;">
            <div class="comp-row">
              <figure class="comp-fig">
                <div class="comp-img-wrap">
                  <img src="/work/search/airbnb.png" alt="Airbnb mobile app — inline photo gallery on search results" class="comp-img" loading="lazy" />
                </div>
                <figcaption><strong>Airbnb</strong> — inline photo gallery lets users swipe photos without opening a listing</figcaption>
              </figure>
              <figure class="comp-fig">
                <div class="comp-img-wrap">
                  <img src="/work/search/homeaway.png" alt="HomeAway mobile app — quick favorite from results list" class="comp-img" loading="lazy" />
                </div>
                <figcaption><strong>HomeAway</strong> — one tap to favorite a listing without leaving the results page</figcaption>
              </figure>
              <figure class="comp-fig">
                <div class="comp-img-wrap">
                  <img src="/work/search/redfin.png" alt="Redfin mobile app — draw your own search boundary" class="comp-img" loading="lazy" />
                </div>
                <figcaption><strong>Redfin</strong> — draw a custom boundary directly on the map</figcaption>
              </figure>
              <figure class="comp-fig">
                <div class="comp-img-wrap">
                  <img src="/work/search/zillow.png" alt="Zillow mobile app — listing card info overlaid on photo" class="comp-img" loading="lazy" />
                </div>
                <figcaption><strong>Zillow</strong> — listing info overlaid on the photo so more cards fit on screen</figcaption>
              </figure>
            </div>
            <div class="comp-row comp-row--details">
              <figure class="comp-fig">
                <div class="comp-img-wrap comp-img-wrap--wide">
                  <img src="/work/search/airbnb-prices.png" alt="Airbnb web — prices shown directly on map pins" class="comp-img" loading="lazy" />
                </div>
                <figcaption><strong>Prices on map</strong> — lets buyers compare neighborhoods without clicking into each listing</figcaption>
              </figure>
              <figure class="comp-fig">
                <div class="comp-img-wrap comp-img-wrap--wide">
                  <img src="/work/search/airbnb-controls.png" alt="Airbnb mobile — filter controls with no keyboard entry" class="comp-img" loading="lazy" />
                </div>
                <figcaption><strong>No keyboard required</strong> — stepper and toggle controls keep filtering fast on mobile</figcaption>
              </figure>
              <figure class="comp-fig">
                <div class="comp-img-wrap comp-img-wrap--wide">
                  <img src="/work/search/homeaway-sliders.png" alt="HomeAway web — sliders for quick filter adjustments" class="comp-img" loading="lazy" />
                </div>
                <figcaption><strong>Sliders</strong> — allow quick range adjustments without typing min and max values</figcaption>
              </figure>
            </div>
          </div>
        </div>
      </section>

      <!-- DECISION 2 -->
      <section class="section section--dark">
        <div class="section-inner two-col">
          <div class="col-left">
            <p class="section-eyebrow">Decision 02</p>
            <h2 class="section-heading-dark">Surfaced DriveTime search. Research said commute was a top priority — it was buried.</h2>
            <p class="body-text body-text--muted">DriveTime was a feature that already existed. You could search by commute time instead of location. But it was hidden, and almost no one used it.</p>
            <p class="body-text body-text--muted">Research made it clear: proximity to work was one of the four things homebuyers cared about most. So we brought DriveTime up front — a first-class tab alongside standard search, not a buried option.</p>
          </div>
          <div class="col-right col-right--vision">
            <div class="vision-card">
              <p class="vision-label">The insight</p>
              <p class="vision-text">Commute time is one of the top 4 homebuyer priorities. It was buried in the current product.</p>
            </div>
            <div class="vision-card">
              <p class="vision-label">The decision</p>
              <p class="vision-text">Promoted DriveTime to a primary tab alongside location search. Made the commute use case first-class.</p>
            </div>
            <div class="vision-card">
              <p class="vision-label">The principle behind it</p>
              <p class="vision-text">Design should reflect what users actually care about — not what's easiest to build or what already exists in the nav.</p>
            </div>
          </div>
        </div>
        <div class="section-inner" style="margin-top: 64px;">
          <figure class="full-bleed-fig">
            <img src="/work/search/drivetime.png" alt="DriveTime search design — tab prominently surfaced alongside standard location search, with map boundary drawing" class="full-bleed-img" loading="lazy" />
            <figcaption>DriveTime promoted to a first-class tab. Buyers could now search by commute time — the thing they actually cared about — without hunting for a buried control.</figcaption>
          </figure>
        </div>
      </section>

      <!-- DESIGN CHALLENGE -->
      <section class="section section--light">
        <div class="section-inner two-col">
          <div class="col-left">
            <p class="section-eyebrow-light">The design challenge</p>
            <h2 class="section-heading">Generic, but not lifeless.</h2>
            <p class="body-text">This was a white-labeled product. Every brokerage had their own brand — colors, logos, voice. We couldn't design for one brand. We had to design for all of them.</p>
            <p class="body-text">No style guide to start from. I built a responsive page structure first so engineering could move, then focused on a visual system that could absorb any brokerage's branding without falling apart. The answer was restraint: neutral base, emphasis on photography, clean type hierarchy.</p>
            <p class="body-text">The constraint forced better decisions. When you can't rely on brand color to do visual work, you have to design with structure.</p>
          </div>
          <div class="col-right">
            <div class="design-principles">
              <div class="principle-item">
                <p class="principle-title">Photo rich</p>
                <p class="principle-body">Large photos not only look better — research shows they help buyers visualize themselves in a home. Photos do the selling. The UI should get out of the way.</p>
              </div>
              <div class="principle-item">
                <p class="principle-title">Lightning fast</p>
                <p class="principle-body">The previous version was slow and users complained. Speed wasn't a nice-to-have — it was a table-stakes requirement given what Zillow had already trained users to expect.</p>
              </div>
              <div class="principle-item">
                <p class="principle-title">No keyboard required</p>
                <p class="principle-body">Filters used text inputs that forced mobile users to type. We replaced them with sliders, toggles, and steppers. No keyboard for standard filtering tasks.</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- DESIGNS -->
      <section class="section section--dark">
        <div class="section-inner">
          <p class="section-eyebrow">The designs</p>
          <h2 class="section-heading-dark">Mobile-first, photo-rich, no keyboard required.</h2>
          <p class="body-text body-text--muted" style="max-width: 640px; margin-bottom: 48px;">Three views, designed around how buyers actually search: list for scanning, map for exploring neighborhoods, and filters that worked without typing.</p>
          <div class="screens-row" style="margin-bottom: 32px;">
            <figure class="screen-fig">
              <img src="/work/search/comp-list.png" alt="List view with inline photo gallery and listing details" class="screen-img" loading="lazy" />
              <figcaption><strong>List view</strong> — large photos with an inline gallery. Buyers can swipe through shots without losing their place in results.</figcaption>
            </figure>
            <figure class="screen-fig">
              <img src="/work/search/comp-search.png" alt="Location search with autocomplete suggestions" class="screen-img" loading="lazy" />
              <figcaption><strong>Location search</strong> — autocomplete reduces friction on mobile, where typing is the biggest barrier to getting started.</figcaption>
            </figure>
            <figure class="screen-fig">
              <img src="/work/search/comp-map.png" alt="Map view with price bubbles on listing pins" class="screen-img" loading="lazy" />
              <figcaption><strong>Map view</strong> — price bubbles let buyers compare neighborhoods at a glance before opening a single listing.</figcaption>
            </figure>
          </div>
          <div class="screens-row screens-row--two">
            <figure class="screen-fig">
              <img src="/work/search/comp-filters.png" alt="Filter panel with price histogram, beds, bath, property type — no keyboard required" class="screen-img" loading="lazy" />
              <figcaption><strong>Filters</strong> — sliders, steppers, and toggles replace every text input. No keyboard required for any standard filter task.</figcaption>
            </figure>
            <figure class="screen-fig">
              <img src="/work/search/comp-saved.png" alt="Saved searches with daily email alerts" class="screen-img" loading="lazy" />
              <figcaption><strong>Saved searches</strong> — buyers get daily email updates on new listings that match their criteria, keeping agents top of mind.</figcaption>
            </figure>
          </div>
        </div>
      </section>

      <!-- TESTING -->
      <section class="section section--dark">
        <div class="section-inner">
          <p class="section-eyebrow">Test and learn</p>
          <h2 class="section-heading-dark">First remote usability study we'd ever run. It found two real problems.</h2>
          <p class="body-text body-text--muted" style="max-width: 640px; margin-bottom: 48px;">Getting participants scheduled had always been a pain point for a small team. This time we used a remote testing service for the first time. We tested on a real dev environment across three focus areas: search and filtering, map controls, and drawing custom boundaries.</p>
          <div class="findings-grid">
            <div class="finding-item">
              <p class="finding-num">01</p>
              <p class="finding-title">Filters were being applied prematurely</p>
              <p class="finding-body">Users set a minimum price, applied it, then came back to set a maximum. The filter UI didn't make it clear you could set a range in one step. You could sense the frustration from the recordings. We fixed the interaction model.</p>
            </div>
            <div class="finding-item">
              <p class="finding-num">02</p>
              <p class="finding-title">Map controls were invisible</p>
              <p class="finding-body">Schools, draw, and boundary tools were at the bottom of the map. Nobody saw them. By far the biggest usability issue. We redesigned the control placement before launch.</p>
            </div>
            <div class="finding-item">
              <p class="finding-num">03</p>
              <p class="finding-title">The core experience landed</p>
              <p class="finding-body">One participant said unprompted: "Definitely a lot quicker than Zillow, and slicker." That was the bar we were aiming for. The positive reactions gave us confidence to ship.</p>
            </div>
          </div>
          <div class="usertesting-row" style="margin-top: 56px;">
            <figure class="usertesting-fig">
              <img src="/work/search/usertesting-1.png" alt="UserTesting recording — user applies minimum price filter then goes back to add maximum" class="full-bleed-img" loading="lazy" />
              <figcaption>Filters applied prematurely — users set a minimum price, applied, then came back for the maximum.</figcaption>
            </figure>
            <figure class="usertesting-fig">
              <img src="/work/search/usertesting-2.png" alt="UserTesting recording — map controls at bottom of map go completely unnoticed" class="full-bleed-img" loading="lazy" />
              <figcaption>Map controls at the bottom of the map were invisible to users.</figcaption>
            </figure>
          </div>
        </div>
      </section>

      <!-- PULL QUOTE -->
      <section class="quote-section">
        <div class="quote-inner">
          <blockquote class="pull-quote">
            "Definitely a lot quicker than Zillow, and slicker."
            <cite class="pull-cite">— Usability study participant</cite>
          </blockquote>
        </div>
      </section>

      <!-- KEY RESULTS -->
      <section class="section section--light">
        <div class="section-inner">
          <h2 class="results-heading">Results</h2>
          <p class="results-subhead">Mobile-first focus paid off where it mattered most.</p>
          <div class="results-grid">
            <div class="result-item">
              <p class="result-num">92%</p>
              <p class="result-label">of owners and managers satisfied or extremely satisfied with the redesign</p>
            </div>
            <div class="result-item">
              <p class="result-num">+36.7%</p>
              <p class="result-label">change in unique pageviews to search on mobile devices</p>
            </div>
            <div class="result-item">
              <p class="result-num">−20.7%</p>
              <p class="result-label">change in bounce rate on mobile devices</p>
            </div>
          </div>
        </div>
      </section>

      <!-- REFLECTION -->
      <section class="section section--dark">
        <div class="section-inner two-col">
          <div class="col-left">
            <p class="section-eyebrow">What I'd do differently</p>
            <h2 class="section-heading-dark">Get users in the room earlier.</h2>
            <p class="body-text body-text--muted">We made excuses for not doing user research earlier in the process. By the time we ran the usability study, the design was already mostly baked. We found real issues — and fixed them — but we could have saved significant time if we'd tested with users during the iterate phase instead of at the end of it.</p>
            <p class="body-text body-text--muted">This project also introduced remote usability testing to the team for the first time. That practice carried forward into every project after it.</p>
          </div>
          <div class="col-right col-right--stat">
            <div class="team-stat">
              <p class="team-stat-num">1st</p>
              <p class="team-stat-label">remote usability study the team had ever run</p>
            </div>
          </div>
        </div>
      </section>

      <!-- NEXT PROJECT -->
      <section class="next-section">
        <div class="next-inner">
          <p class="next-label">More from MoxiWorks</p>
          <h2 class="next-headline">How I built a UX culture where there was none.</h2>
          <a class="next-cta" href="/work/moxiworks">The leadership story →</a>
        </div>
      </section>

    </div>

  </body>
</html>

<script is:inline>
  function init() {
    const backBtn = document.getElementById('back-btn');
    backBtn?.addEventListener('click', (e) => {
      if (document.referrer.startsWith(window.location.origin)) {
        e.preventDefault();
        history.back();
      }
    });
  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
</script>

<style>
  body { background: #201e1b; overflow-x: hidden; }

  .bg-layer { position: fixed; inset: 0; z-index: 1; pointer-events: none; }
  .bg-orbs  { position: absolute; inset: 0; }
  .bg-grain { position: absolute; inset: 0; mix-blend-mode: overlay; opacity: 0.22; background-image: url("data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' width='300' height='300'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/></filter><rect width='300' height='300' filter='url(%23n)' opacity='1'/></svg>"); background-size: 300px 300px; }

  @keyframes pulse-a { from { opacity: 0.1; transform: scale(1); } to { opacity: 0.6; transform: scale(1.1); } }
  @keyframes pulse-b { from { opacity: 0.05; transform: scale(1); } to { opacity: 0.45; transform: scale(1.12); } }
  .orb { position: absolute; border-radius: 50%; mix-blend-mode: screen; }
  .orb-a { width: 600px; height: 600px; top: -100px; left: -100px; background: rgba(201,169,110,0.18); filter: blur(80px); animation: pulse-a 6s ease-in-out infinite alternate; }
  .orb-b { width: 500px; height: 500px; top: 40%; right: -80px; background: rgba(242,237,232,0.08); filter: blur(80px); animation: pulse-b 8s ease-in-out infinite alternate; }
  .orb-c { width: 400px; height: 400px; bottom: 10%; left: 20%; background: rgba(201,169,110,0.1); filter: blur(100px); animation: pulse-a 7s ease-in-out infinite alternate; animation-delay: 2s; }

  .cs-content { display: block; }

  /* Hero two-column layout */
  .hero-inner { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(32px, 5vw, 80px); align-items: end; overflow: visible; }
  .hero-text { padding-bottom: clamp(64px, 10vh, 120px); }
  .hero-phone { position: relative; margin-bottom: -80px; align-self: end; }
  .hero-phone-img { width: 100%; height: auto; display: block; filter: drop-shadow(0 32px 64px rgba(0,0,0,0.5)); }

  @media (max-width: 768px) {
    .hero-inner { grid-template-columns: 1fr; }
    .hero-phone { display: none; }
    .hero-text { padding-bottom: clamp(40px, 6vh, 80px); }
  }
  .page-container { max-width: 1080px; margin: 0 auto; }

  .back-wrap { position: relative; z-index: 10; padding: 104px clamp(28px, 8vw, 120px) 0; }
  .back-link { display: inline-flex; align-items: center; gap: 6px; font-family: var(--font-sans); font-size: 12px; font-weight: 500; letter-spacing: 0.06em; text-transform: uppercase; color: rgba(242,237,232,0.4); text-decoration: none; transition: color 0.2s ease; }
  .back-link:hover { color: #C9A96E; }

  .hero { position: relative; z-index: 10; padding: clamp(32px, 5vh, 56px) clamp(28px, 8vw, 120px) 0; overflow: visible; }
  .hero-inner { max-width: 1080px; margin: 0 auto; }
  .eyebrow { font-family: var(--font-sans); font-size: 11px; font-weight: 600; letter-spacing: 0.18em; text-transform: uppercase; color: #C9A96E; margin: 0 0 24px; }
  .hero-headline { font-family: var(--font-serif); font-weight: 300; font-size: clamp(36px, 5.5vw, 72px); line-height: 1.1; letter-spacing: -0.025em; color: #f2ede8; margin: 0 0 48px; max-width: 860px; }
  .hero-headline em { font-style: italic; color: #C9A96E; }
  .meta-strip { display: flex; align-items: center; gap: 32px; flex-wrap: wrap; }
  .meta-item { display: flex; flex-direction: column; gap: 4px; }
  .meta-label { font-family: var(--font-sans); font-size: 10px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: rgba(242,237,232,0.35); margin: 0; }
  .meta-value { font-family: var(--font-sans); font-size: 14px; color: rgba(242,237,232,0.75); margin: 0; }
  .meta-divider { width: 1px; height: 32px; background: rgba(242,237,232,0.12); }

  .section { position: relative; z-index: 10; padding: clamp(64px, 10vh, 120px) clamp(28px, 8vw, 120px); }
  .section--light { background: #f2ede8; }
  .section--dark  { background: #1a1815; }
  .section-inner  { max-width: 1080px; margin: 0 auto; }
  .two-col { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(40px, 6vw, 96px); align-items: start; }

  .section-eyebrow { font-family: var(--font-sans); font-size: 11px; font-weight: 600; letter-spacing: 0.18em; text-transform: uppercase; color: #C9A96E; margin: 0 0 16px; }
  .section-eyebrow-light { font-family: var(--font-sans); font-size: 11px; font-weight: 600; letter-spacing: 0.18em; text-transform: uppercase; color: rgba(43,43,43,0.4); margin: 0 0 16px; }
  .section-heading { font-family: var(--font-serif); font-weight: 300; font-size: clamp(28px, 3.5vw, 48px); line-height: 1.15; letter-spacing: -0.02em; color: #2b2b2b; margin: 0 0 24px; }
  .section-heading-dark { font-family: var(--font-serif); font-weight: 300; font-size: clamp(24px, 3vw, 42px); line-height: 1.15; letter-spacing: -0.02em; color: #f2ede8; margin: 0 0 24px; }

  .body-text { font-family: var(--font-sans); font-size: clamp(15px, 1.2vw, 17px); line-height: 1.75; color: #595959; margin: 0 0 18px; }
  .body-text:last-child { margin-bottom: 0; }
  .body-text--muted { color: rgba(242,237,232,0.5); }

  .objective-box { background: #2b2b2b; border-radius: 10px; padding: 28px 32px; }
  .objective-label { font-family: var(--font-sans); font-size: 10px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: #C9A96E; margin: 0 0 12px; }
  .objective-quote { font-family: var(--font-serif); font-style: italic; font-size: clamp(15px, 1.3vw, 19px); line-height: 1.55; color: #f2ede8; margin: 0; }

  /* Decision body */
  .decision-body { max-width: 720px; }

  /* Full bleed figures */
  .full-bleed-fig { margin: 0; }
  .full-bleed-img { width: 100%; height: auto; display: block; border-radius: 10px; border: 1px solid rgba(0,0,0,0.08); }
  .section--dark .full-bleed-img { border-color: rgba(255,255,255,0.06); }
  figcaption { font-family: var(--font-sans); font-size: 12px; line-height: 1.6; color: #6b6560; margin-top: 12px; text-align: center; }
  .section--dark figcaption { color: rgba(242,237,232,0.6); }

  /* Competitor grid */
  .comp-grid { display: flex; flex-direction: column; gap: 24px; }
  .comp-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }
  .comp-row--details { grid-template-columns: repeat(3, 1fr); }
  .comp-fig { margin: 0; }
  .comp-img-wrap { overflow: hidden; border-radius: 8px; border: 1px solid rgba(0,0,0,0.08); aspect-ratio: 9/16; }
  .comp-img-wrap--wide { aspect-ratio: 4/3; }
  .comp-img { width: 100%; height: 100%; display: block; object-fit: cover; object-position: top; }

  /* Mobile screen rows */
  .screens-row { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; align-items: start; }
  .screens-row--two { grid-template-columns: repeat(2, 1fr); max-width: 680px; }
  .screen-fig { margin: 0; }
  .screen-img { width: 100%; height: auto; display: block; border-radius: 8px; border: none; }

  /* UserTesting side by side */
  .usertesting-row { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
  .usertesting-fig { margin: 0; }

  @media (max-width: 640px) {
    .comp-row { grid-template-columns: repeat(2, 1fr); }
    .comp-row--details { grid-template-columns: 1fr; }
    .screens-row { grid-template-columns: 1fr; }
    .usertesting-row { grid-template-columns: 1fr; }
  }

  /* Insights grid */
  .insights-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 2px; border-radius: 10px; overflow: hidden; }
  .insight-item { background: #ede8e1; padding: clamp(28px, 3vw, 40px); }
  .insight-label { font-family: var(--font-sans); font-size: 10px; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(43,43,43,0.38); margin: 0 0 10px; }
  .insight-title { font-family: var(--font-sans); font-size: 15px; font-weight: 600; color: #2b2b2b; margin: 0 0 10px; }
  .insight-body { font-family: var(--font-sans); font-size: 14px; line-height: 1.65; color: rgba(43,43,43,0.6); margin: 0; }

  /* Vision cards */
  .col-right--vision { display: flex; flex-direction: column; gap: 12px; }
  .vision-card { background: rgba(242,237,232,0.05); border: 1px solid rgba(242,237,232,0.08); border-radius: 8px; padding: 20px 24px; }
  .vision-label { font-family: var(--font-sans); font-size: 10px; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: #C9A96E; margin: 0 0 8px; }
  .vision-text { font-family: var(--font-sans); font-size: 14px; line-height: 1.6; color: rgba(242,237,232,0.65); margin: 0; }

  /* Design principles */
  .design-principles { display: flex; flex-direction: column; gap: 0; border-radius: 10px; overflow: hidden; }
  .principle-item { background: #ede8e1; padding: 28px 32px; border-bottom: 2px solid #f2ede8; }
  .principle-item:last-child { border-bottom: none; }
  .principle-title { font-family: var(--font-sans); font-size: 14px; font-weight: 600; color: #2b2b2b; margin: 0 0 8px; }
  .principle-body { font-family: var(--font-sans); font-size: 14px; line-height: 1.65; color: rgba(43,43,43,0.6); margin: 0; }

  /* Findings */
  .findings-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; border-radius: 10px; overflow: hidden; }
  .finding-item { background: rgba(242,237,232,0.04); border: 1px solid rgba(242,237,232,0.08); padding: clamp(28px, 3vw, 40px); }
  .finding-num { font-family: var(--font-sans); font-size: 11px; font-weight: 600; letter-spacing: 0.1em; color: rgba(242,237,232,0.2); margin: 0 0 16px; }
  .finding-title { font-family: var(--font-sans); font-size: 15px; font-weight: 600; color: #f2ede8; margin: 0 0 12px; }
  .finding-body { font-family: var(--font-sans); font-size: 14px; line-height: 1.65; color: rgba(242,237,232,0.45); margin: 0; }

  /* Pull quote */
  .quote-section { position: relative; z-index: 10; background: #201e1b; padding: clamp(64px, 10vh, 120px) clamp(28px, 8vw, 120px); border-top: 1px solid rgba(242,237,232,0.06); border-bottom: 1px solid rgba(242,237,232,0.06); }
  .quote-inner { max-width: 800px; margin: 0 auto; }
  .pull-quote { font-family: var(--font-serif); font-style: italic; font-weight: 300; font-size: clamp(24px, 3.5vw, 44px); line-height: 1.35; letter-spacing: -0.015em; color: #f2ede8; margin: 0 0 16px; border-left: 3px solid #C9A96E; padding-left: clamp(24px, 3vw, 48px); }
  .pull-cite { display: block; font-family: var(--font-sans); font-size: 13px; font-style: normal; color: rgba(242,237,232,0.3); padding-left: clamp(24px, 3vw, 48px); margin-top: 8px; }

  /* Results */
  .results-heading { font-family: var(--font-serif); font-weight: 300; font-size: clamp(28px, 3.5vw, 48px); letter-spacing: -0.02em; color: #2b2b2b; margin: 0 0 8px; text-align: center; }
  .results-subhead { font-family: var(--font-sans); font-size: 14px; color: rgba(43,43,43,0.45); text-align: center; margin: 0 0 56px; }
  .results-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; border-radius: 10px; overflow: hidden; }
  .result-item { background: #ede8e1; padding: clamp(32px, 4vw, 56px) clamp(24px, 3vw, 40px); text-align: center; }
  .result-num { font-family: var(--font-sans); font-weight: 700; font-size: clamp(36px, 5vw, 64px); line-height: 1; letter-spacing: -0.04em; color: #2b2b2b; margin: 0 0 12px; }
  .result-label { font-family: var(--font-sans); font-size: 13px; line-height: 1.55; color: rgba(43,43,43,0.5); margin: 0 auto; max-width: 200px; }

  /* Team stat */
  .col-right--stat { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 8px; padding: 48px 32px; background: rgba(242,237,232,0.04); border: 1px solid rgba(242,237,232,0.08); border-radius: 12px; }
  .team-stat { text-align: center; }
  .team-stat-num { font-family: var(--font-sans); font-weight: 600; font-size: clamp(48px, 6vw, 80px); line-height: 1; letter-spacing: -0.04em; color: #f2ede8; margin: 0 0 6px; }
  .team-stat-label { font-family: var(--font-sans); font-size: 13px; line-height: 1.5; letter-spacing: 0.02em; color: rgba(242,237,232,0.35); margin: 0; max-width: 180px; text-align: center; }

  /* Next */
  .next-section { position: relative; z-index: 10; background: #171512; padding: clamp(64px, 10vh, 120px) clamp(28px, 8vw, 120px); }
  .next-inner { max-width: 1080px; margin: 0 auto; }
  .next-label { font-family: var(--font-sans); font-size: 11px; font-weight: 600; letter-spacing: 0.18em; text-transform: uppercase; color: rgba(242,237,232,0.3); margin: 0 0 16px; }
  .next-headline { font-family: var(--font-serif); font-weight: 300; font-size: clamp(24px, 3.5vw, 48px); line-height: 1.15; letter-spacing: -0.02em; color: #f2ede8; margin: 0 0 32px; max-width: 600px; }
  .next-cta { font-family: var(--font-sans); font-size: 14px; font-weight: 500; color: #C9A96E; text-decoration: none; border-bottom: 1px solid rgba(201,169,110,0.35); padding-bottom: 2px; transition: color 0.2s ease, border-color 0.2s ease; }
  .next-cta:hover { color: #d9bc89; border-color: rgba(201,169,110,0.7); }

  @media (max-width: 768px) {
    .two-col { grid-template-columns: 1fr; }
    .insights-grid { grid-template-columns: 1fr; }
    .findings-grid { grid-template-columns: 1fr; }
    .results-grid { grid-template-columns: 1fr; }
    .meta-strip { gap: 20px; }
    .meta-divider { display: none; }
    .col-right--stat { padding: 32px 24px; }
  }
</style>
